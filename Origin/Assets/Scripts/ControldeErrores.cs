using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControldeErrores : MonoBehaviour
{
    int[] array;
    // Start is called before the first frame update
    void Start()
    {
        array = new int[3];
        array[0] = 1;
        array[1] = 19;
        array[2] = -81;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
